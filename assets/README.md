# Assets

- `scans/`: source GLBs, converted USDs, and scan textures.
- `convert_scans_to_usd.py`: Isaac asset-converter entry point.

Keep textures beside the scans so relative material paths remain valid.
